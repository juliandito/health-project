apiVersion: v1
kind: Service
metadata:
  name: mock-ai-service
  namespace: healthcare-triage-dev
  labels:
    app: mock-ai-service
    environment: dev
spec:
  type: ClusterIP
  selector:
    app: mock-ai-service
  ports:
    - name: http
      port: 8000
      targetPort: http
